# Source: ITR_TimeFlow_Production's Mongo (read-only during migration)
TIMEFLOW_MONGO_URI=
TIMEFLOW_DB_NAME=timesheet_db

# Source: Flow_Tracker's Mongo (read-only during migration)
FLOWTRACKER_MONGO_URI=
FLOWTRACKER_DB_NAME=

# Target: new unified database. Point this at a staging cluster/db until
# Phase 5 cutover — never point it at a source database.
TARGET_MONGO_URI=
TARGET_DB_NAME=itr_one
